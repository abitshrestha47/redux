
//the incremnet funciton of type increment to perform action in reducers i.e case 
export const increment=()=>({
    type:'INCREMENT',
});


//the decrement function of type decrement to perform action in reducers i.e case
export const decrement=()=>({
    type:'DECREMENT',
});